.vercel
node_modules/

# Torrent download cache (disposable runtime data; see server/torrents.js)
data/torrents/

# Vendored external repository (development reference only)
serviceradar/

# Build artifacts
wasm/afterlight-realtime/target/
server_elixir/_build/
server_elixir/deps/
server_elixir/erl_crash.dump
*.dump
/erl_crash.dump
*.beam

# Environment files
.env
.env.local
.env.*.local
.env.production
.env*
!.env.development
!.env.production.example
!.env*.example

# Keys, certificates, and secrets
*.pem
*.key
*.p12
*.pfx

# OS metadata
.DS_Store
Thumbs.db

# Local agent tooling caches (video2code / video-agent-kit plugin pointers)
.v2c/
.video_agent/
server_elixir/.video_agent/

# Vendored external repository (own git repo; Alpine Rush prototype)
SSXTricky/
.dream-loop*/

# Dated pre-drop economy archive (retirement; never the protected snapshots)
.retired/
.dream-loop/
afterlight-elixir-architecture.7z

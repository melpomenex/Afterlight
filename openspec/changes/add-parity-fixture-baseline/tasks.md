## 1. Exporter pipeline

- [ ] 1.1 Create `scripts/export-parity-fixtures.mjs`: writes JSON fixture files to `tests/fixtures/parity/` with an `--out <dir>` flag (default the repo path), stable serialization (fixed key insertion order, 2-space indent, trailing newline), the per-case shape `{fn, args, state, nowMs, seed?, expected, error?}` per `docs/architecture/elixir/parity-notes.md` §2, a `maskGenerated()` helper emitting `"<generated>"` for theater `newItemId`, iptv `newId`, orderbook trade ids, and nickname random fallbacks, and a sorted `manifest.json` (functions per file + hazard-class map).
- [ ] 1.2 Give the exporter a pure entry point importable by tests (e.g. an `exportFixtures(outDir)` function alongside the CLI wrapper) so the determinism test can call it twice in-process without spawning.

## 2. Fixture sets (parity-notes §3 priorities 1–10)

- [ ] 2.1 `tests/fixtures/parity/theater-model.json`: `applyTheaterAction` full op matrix (add, addMany, remove, playNow, skip, clear — success and error outcomes with exact reason strings incl. `queue_full`, `item_mismatch`), `classifySource` URL table (~40 cases incl. hostile: wrong scheme, backslashes, uppercase hosts, searchParams, over-length, garbage), `parseM3U` (attrs incl. `tvg-id`, damaged lines, skipped counts), `normalizeTheaterState` on null / null-field / absent-field shapes (pins the `Number(null)` vs `Number(undefined)` hazard), and `effectivePositionSec` at fixed nowMs values `1_700_000_000_000` and `1_700_000_060_000` across playing and paused states.
- [ ] 2.2 `tests/fixtures/parity/torrent-model.json`: `parseMagnet` with hex and base32 infohashes (40 lowercase hex out; xt/btrm + parameter edge cases), `sanitizeTorrentPick` boundaries (fileIndex 1_000_000, float `fileBytes`, missing fields, null vs undefined vs absent), `orderFilesForPicker` tie-stability (equal bytes keep insertion order, playable-first, picker cap), `normalizeTorrentStatus` (string/number coercion, missing fields), and `parseRange` from `server/torrents.js` (valid, suffix, open-ended, malformed, unsatisfiable → null).
- [ ] 2.3 `tests/fixtures/parity/garden-crops.json`: `tickBed` dt=1 multi-step sequences (moisture decay with and without rain and sprinkler — float accumulation pinned through the sequence), `harvestBed` across `calculateQuality` boundaries 0.9/0.75/0.55, `sprinklerCoverage` corner and edge bed indices (0, cols−1, first/last row), `getGrowthStage` at exact stage-boundary ms values (±1 ms), and a `calculateQuality` table over moisture-history/health combinations.
- [ ] 2.4 `tests/fixtures/parity/economy.json`: `calculateNpcSellPrice` across all sellable goods × qualities × multipliers {0.4, 0.85, 1.0, 1.0005, 2.5} (pins `Math.round` half-toward-+∞ and `Number(x.toFixed(3))` binary-float formatting), `calculateNpcSeedPrice` across crops × multipliers, and `updateMarketMultiplier` step chains as multi-step scripts (clamp at bounds, drift pinned through JSON round-trip).
- [ ] 2.5 `tests/fixtures/parity/orderbook.json`: `OrderBook` scripts with a stub storage and injected `createdAt` — price-time priority, partial fills, marketable limit executing at the maker's price, fee `Math.max(1, Math.round(tradeValue*0.02))` including the tradeValue=25 min-fee boundary and tradeValue=125 (`round(2.5)` = 3 in JS vs 2 under half-even — a real port divergence), crop-mismatch orders breaking (not skipping) matching, self-match allowed, cross-side book states via `getBookSnapshot`, and `cancelOrder` with escrow-relevant state in the output; multi-step `{steps: [...]}` scripts threading book state.
- [ ] 2.6 `tests/fixtures/parity/iptv-xmltv.json`: `applyAddPlaylist` at fixed nowMs with names that never hit the `toLocaleDateString` fallback, `sanitizeChannels` (cap enforcement, malformed rows dropped and counted), `applySetEpg`/`sanitizeGuideProgrammes` with unsorted programme input → sorted output and cap behavior, `serializeM3U` ↔ `parseM3U` round-trip, `catalogSnapshot` metadata shape, `parseXmltvTime` timezone-offset table including `Date.UTC` rollover cases (month 13, day 0, hour 25), `normalizeChannelKey`, `createEpgIndex` first-wins on duplicate ids (key-order hazard), `nowNextForId` at exact start/end boundaries, and `lookupNowNext` cap (300) + input-order preservation.
- [ ] 2.7 `tests/fixtures/parity/identity.json`: `generateDefaultNickname` seeds {0, 0.1, 0.5, 0.999}, `sanitizeNickname` including UTF-16 code-unit truncation with astral characters and emoji, `resolveDuplicateNickname` ladder up to 99 (…98, 99, then failure), and `generatePlayerPalette` including astral characters and the `>>> 0` 32-bit mask wrap-around.
- [ ] 2.8 `tests/fixtures/parity/nodes-machines.json`: node harvest / `isDepleted` / respawn tick at fixed now values (depletedAt boundary ±1 ms), `MachinesManager.contribute` clamp matrix (requested vs held vs remaining, broken mill, over-contribution, restore-in-same-step), `millWheat` consumption order C→B→A→A+ across inventory quality ladders, and sprinkler craft (copper 2 + glass 2, insufficient materials rejected).
- [ ] 2.9 `tests/fixtures/parity/youtube-playlist.json`: `extractPlaylistVideos` over synthetic HTML fixture strings (ytInitialData shapes, DFS collect order per the key-order hazard, damaged payloads → counted skips), `isYouTubeMixId`, and `looksLikePlaylistId` length/charset boundaries.
- [ ] 2.10 `tests/fixtures/parity/protocol.json`: `protocol.parse` on malformed input (truncated JSON, non-JSON text, empty string → null) and `protocol.serialize` round-trips for representative pinned message shapes.

## 3. Determinism gate

- [ ] 3.1 Create `tests/parity-fixtures.test.js`: exports twice into temporary directories and asserts byte-identical output; asserts committed `tests/fixtures/parity/*.json` equal a fresh export; asserts the manifest maps every parity-notes §1 hazard class to at least one case id; runs under plain `node --test` so the `npm test` glob picks it up.
- [ ] 3.2 Run the exporter, commit the generated `tests/fixtures/parity/*.json`, and confirm a second export leaves `git status` clean (byte stability in practice, not just in the test).

## 4. Node baseline measurements

- [ ] 4.1 Add the opt-in measurement probe `server/baselineProbe.js` (active only with `AFTERLIGHT_BASELINE_PROBE=1`): times `storage.save()` calls and samples `perf_hooks.monitorEventLoopDelay`, emitting batched JSONL diagnostics to stderr; dead code when unset; wire it into `server/index.js` with a minimal env check; no gameplay or protocol effect.
- [ ] 4.2 Create `scripts/measure-node-baseline.mjs`: boots the real server on a scratch port with throwaway state, drives two scripted WS clients through hello/join/movement, records room broadcast frame byte sizes over a fixed window (p50/p95/max per flush and per second), consumes the probe stream, and prints a summary; includes a no-probe run to confirm the probe does not distort traffic numbers.
- [ ] 4.3 Run the harness and record results honestly in `docs/architecture/elixir/baseline-node.md` (method, environment, repo revision, raw distributions); mark anything not measurable in this environment as "not measured" with the reason — no invented numbers, no capacity targets.

## 5. Elixir parity runner (executable once `add-elixir-phoenix-foundation` provides the Mix app)

- [ ] 5.1 Create `server_elixir/test/parity/` with the `Afterlight.Parity` runner: loads `tests/fixtures/parity/manifest.json` + case files from the repo root (path resolved relative to the Mix project and taken from config), walks the manifest, dispatches fixture file → reference module, applies the numeric-tolerant / order-sensitive comparators, and fails with fixture file + case id on any mismatch.
- [ ] 5.2 Implement the `Afterlight.Parity.Reference.*` ports for every pinned function plus the shared hazard helpers (`js_round/1` as floor(x + 0.5), `js_to_number/1`, UTF-16 code-unit truncation, 32-bit mask, `Date.UTC` rollover semantics); name and document them as test-side parity references, never authority.
- [ ] 5.3 Wire the parity suite into `mix test` (config paths + test helper) and verify it is green against the committed fixtures from a clean checkout with no Node runtime available at test time.

## 6. Verification

- [ ] 6.1 `node scripts/export-parity-fixtures.mjs` runs clean from the pinned revision and a re-run leaves `git status` clean.
- [ ] 6.2 `node --test tests/parity-fixtures.test.js` passes and the full `npm test` stays green with no edits to existing tests.
- [ ] 6.3 Walk the hazard checklist: each parity-notes §1 class traces to at least one committed case via the manifest.
- [ ] 6.4 After `add-elixir-phoenix-foundation` lands the Mix app: `mix test` green including the parity suite; record the fixture-run evidence required by the migration-governance parity gate.
- [ ] 6.5 Review `docs/architecture/elixir/baseline-node.md` for honesty: method and revision present for every number, unmeasured quantities marked, no unsourced figures.

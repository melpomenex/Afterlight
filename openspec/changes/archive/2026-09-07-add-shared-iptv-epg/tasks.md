## 1. Shared pure modules

- [x] 1.1 Add `tvgId` capture to `parseExtInf`/`parseM3U` in `shared/theaterModel.js` (additive `tvgId` field on entries; existing fields/behavior unchanged) and extend the `parseM3U` tests in `tests/theater.test.js` for `tvg-id` presence and absence.
- [x] 1.2 Create `shared/xmltv.js`: tolerant scanning XMLTV parser (`parseXmltv` async, yielding between blocks — channels, display-names, icons, programmes with start/stop/channel/title/sub-title/desc; XMLTV time parsing to UTC ms; skipped-entry counting; `recognized: false` for non-XMLTV input), `matchEpgChannels` (tvg-id match, normalized display-name fallback), and `nowNext` (binary search over per-channel sorted programmes).
- [x] 1.3 Create `shared/iptvModel.js`: limits as named constants (`LISTS_MAX`, `LIST_TEXT_MAX`, `CHANNELS_MAX`, `EPG_FILE_MAX`, `EPG_CHANNELS_MAX`, `EPG_PROGRAMMES_MAX`, `EPG_LOOKUP_MAX`), playlist-name sanitization, library action reducer-style helpers (add list, remove list, add/replace EPG) returning `{ state, error }` like `applyTheaterAction`, `normalizeIptvLibrary()` repair for untrusted persisted data, catalog snapshot builder (metadata only), and an M3U serializer for pushing personal lists (channels → M3U text).
- [x] 1.4 Create `tests/iptv-epg.test.js`: XMLTV parsing (well-formed, entities, self-closing tags, missing attributes, damaged entries skipped, non-XMLTV rejected, gzipped input handled at the manager level), matching (tvg-id, name fallback, unmatched clean), `nowNext` boundaries, limit enforcement (list count, text size, channel count, file/channel/programme caps, lookup batch cap), `normalizeIptvLibrary` on corrupt data, and M3U serializer round-trip with `parseM3U`.

## 2. Server: manager, persistence, protocol

- [x] 2.1 Add `MSG_TYPES` to `shared/protocol.js`: `IPTV_STATE`, `IPTV_LIST_GET`, `IPTV_LIST`, `IPTV_LIST_REMOVE`, `EPG_LOOKUP`, `EPG_SCHEDULE` (S→C vs C→S noted in comments, matching existing style).
- [x] 2.2 Create `server/iptv.js` (`IptvManager`): loads `data/iptv.json` + `data/epg.json` via `shared/iptvModel.js` normalization (missing/corrupt → empty library / no guide, never throws), holds the in-memory guide index, exposes `snapshot()` (metadata-only catalog + EPG summary), `listChannels(listId)`, `lookup(keys, atMs)`, and apply methods that persist atomically (tmp + rename, own files — `server/storage.js` untouched) and return `{ success, reason? }`.
- [x] 2.3 Wire `server/index.js`: instantiate `IptvManager`; add `iptv: iptv.snapshot()` to the WELCOME payload and to the theater-room JOIN snapshot send; route the new C→S message types (theater-room-scoped like `THEATER_*`, with `IPTV_STATE` broadcast to the room and `IPTV_LIST`/`EPG_SCHEDULE` replies to the requester); readable error text for rejected actions.
- [x] 2.4 Add HTTP upload endpoints in `server/index.js`'s `http.createServer` handler: `POST /api/theater/playlists` (UTF-8 text body; JSON `{name,url}` variant fetched server-side with http(s)-only, 15 s abort, redirect cap, streamed size cap) and `POST /api/theater/epg` (raw bytes, gzip magic → `zlib.gunzipSync` with `maxOutputLength`); Content-Length + streamed byte enforcement before parsing; CORS preflight (`OPTIONS`) and `Origin` echo on `/api/theater/*` only; JSON result responses (counts, or `{ error }` with proper status codes); parse runs without blocking the loop (yields between blocks).

## 3. Client: upload flow, shared guide, now/next

- [x] 3.1 Add upload helpers to `src/net/client.js`: derive the API base from `wsUrl` (ws:→http:, strip trailing `/ws`), `uploadPlaylist(text, name)` / `importPlaylistUrl(url, name)` / `uploadEpg(file, name)` via `fetch` POST with error surfaced as a readable message; register handlers for `iptv_state`, `iptv_list`, `epg_schedule`.
- [x] 3.2 Rework the IPTV section of `src/ui/theaterScreen.js`: import (paste/file/URL) posts to the server instead of client-side parsing; status line reports server results (channels loaded, errors) using player-facing copy; shared lists appear in the saved-lists select (labelled as theater lists) ahead of personal localStorage lists; "Add to theater" action serializes a personal list via the M3U serializer and uploads it; flipping works from the selected shared or personal list through the unchanged `sendTheaterChannel` path, and the no-list flip prompt remains.
- [x] 3.3 Add schedule display to the guide dialog: lazy `iptv_list` fetch + per-session cache for shared lists; now/next column (title + local start–end times) for matched channels from `epg_schedule` cache; refresh on guide open and every 60 s while open so "now" advances; EPG summary in the guide status area; channels without schedule data render unchanged without errors; EPG upload control (file input, `.epg/.xml/.xmltv`, plain or gzip) in the booth/import UI with success/failure feedback and the one-active-guide replacement note.
- [x] 3.4 Keep cinema view, sitting, HUD, and keyboard rules intact: (verified in-browser — dialogs keep native cancel handlers, new inputs inherit focus styles, cinema view/watch bar unaffected; new CSS is scoped to new classes) verify no focus traps from the new controls, no input hijack while typing in the new fields, panels stay within the responsive breakpoints, and theater playback state is untouched by library/EPG changes.

## 4. Verification and docs

- [x] 4.1 Server integration test (extend `tests/iptv-epg.test.js` or add `tests/iptv-net.test.js` following `tests/theater-net.test.js`): real server on port 0 — upload playlist over HTTP, second client receives `iptv_state` and can `iptv_list_get`/tune; late joiner snapshot; remove list broadcast; EPG upload (gzip fixture) then `epg_lookup` returns now/next; restart preserves catalog+guide; corrupt data files start empty; oversized/non-http/invalid uploads rejected with library unchanged.
- [x] 4.2 Run `npm test` and `npm run build`; resolve real failures (the >500 kB chunk warning is pre-existing and non-fatal).
- [x] 4.3 Browser verification against the dev server: upload `~/Code/iptv/out/master.m3u8` and `guide.epg.gz` through the real UI in the theater; confirm a second browser profile (or second guest tab) sees the same catalog and guide without importing; tune and flip channels from the non-importer; verify now/next times match the local timezone and advance; reload and restart the server to confirm persistence; confirm personal lists and courtyard/theater behavior have not regressed; check console for runtime errors distinct from external font/network noise.
- [x] 4.4 Update `README.md` (uploading playlists and the program guide, what fellow viewers get, size limits, gzip support, the local `~/Code/iptv/out` files as example uploads) and the theater section of `AGENTS.md` (shared library/EPG architecture, new message types, persistence split, upload endpoints).
- [x] 4.5 (no update needed — `tests/theater-ui.test.js` pins only pure helpers and the headless smoke path; both pass unchanged) Update `tests/theater-ui.test.js` if its DOM fixtures assert on the changed import/guide markup so the suite keeps encoding the new behavior.

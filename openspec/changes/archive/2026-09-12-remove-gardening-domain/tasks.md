# remove-gardening-domain — Tasks

## 1. Identity, Naming and Copy

- [x] 1.1 Replace the nickname word banks in `shared/identity.js` with industrial/afterlight adjectives and machine/urban nouns, removing every produce, plant and gardening term; keep the deterministic seed math and 20-char sanitization. Verify with a focused unit test asserting generated names match the new banks and contain no produce term, plus `npm test` for `tests/multiplayer.test.js`.
- [x] 1.2 Mirror the new banks in `server_elixir/lib/afterlight/accounts/reducer_support.ex`, update the parity reference in `scripts/parity/misc.mjs` and regenerate `tests/fixtures/parity/identity-nodes-machines.json`. Verify `npm test` parity and `mix test test/afterlight/accounts/reducer_support_test.exs test/afterlight/accounts/identity_test.exs`.
- [x] 1.3 Add the storage-key migration in `src/net/client.js`: read `afterlight-guest-id`/`afterlight-nickname`, fall back to the legacy `afterlight-gardener-*` values, write the new keys, and never delete the legacy values. Verify with a localStorage mock test covering new, legacy-only and storage-unavailable cases.
- [x] 1.4 Scrub gardener identity: rename `createGardenerAvatar` to `createPlayerAvatar` with a `Visitor` fallback, remove the watering-can prop and `setWateringCan`, change the IRC bridge username default and welcome text to visitor language, reword the heart emote hint, and update the profile/settings/product copy in `index.html`. Verify `rg -i "gardener"` yields only the intentional legacy storage-key fallback and historical migration notes.
- [x] 1.5 Replace `src/ui/marketModal.js` with a nickname-only profile UI: remove market/seed/contract/order/inventory/machine views, the coin/reputation/XP writer, tool state and legacy labels; keep nickname editing and player summaries. Verify the profile opens, saves a nickname, and the header renders no economy values.
- [x] 1.6 Remove `src/ui/placeHudPolicy.js` and its consumers: drop the `data-hud-legacy`/`afterlight-legacy-ui-v1` preference and settings checkbox, the `body[data-hud-context]` CSS hooks, tool-belt/stat-pill/satchel/market/mill markup in `index.html`, the `1–6`/`I`/`M` bindings in `main.js`, and the mill panel. Verify keyboard play (T/E/C/V, WASD, Space) and that no removed surface opens; update/retire `tests/place-hud-policy.test.js`.

## 2. Client Domain Removal

- [x] 2.1 Delete the personal garden: remove `src/world/gardenWorld.js`, `src/render/plants.js`, the garden entries in `src/world/bounds.js` and `src/places/travelState.js` (garden kind, spawns, bed update input), the `gardenRoom` requirement and `garden_gate` registration in `src/social/interactions.js`, and all garden wiring in `src/main.js`. Verify no import of a deleted module remains and `tests/place-travel.test.js`/`tests/seating.test.js` pass with updated expectations.
- [x] 2.2 Strip `src/world/marketWorld.js` to a scenery-only square: remove the trade board, seed vendor, contracts board, mill/machine bench and garden gate while keeping paving, walls, crates and the western `district_gate`; update the Market Court arrival copy and canvas description in `src/main.js` to be neutral. Verify travel to `market`, the west gateway to `canal`, and the default-room fallback still work.
- [x] 2.3 Remove the retired protocol surface: delete the garden/market/order/contract/node/machine client→server and server→client constants from `shared/protocol.js`, the senders/handlers in `src/net/client.js` and `src/main.js`, `garden_state` from `src/net/roomEpoch.js`, and the `gardenFor`/`isGarden`/`gardenOwner` room helpers while keeping `ROOMS.MARKET`. Verify the client ignores an injected retired snapshot without error and `tests/net/room-epoch.test.js`/`tests/load-client.test.js` pass with updated fixtures.
- [x] 2.4 Remove The Glass Garden from the manifest: delete the `garden` entry and minimap path in `shared/placeDefinitions.js`, replace index-derived seeds with an explicit frozen seed table that preserves every remaining id, declare the remaining west/east and south `market` exits so `canal` is adjacent to `station`, and remove the garden builder/palette/minimap registrations in `src/districts.js`/`src/places/worldFactory.js`. Verify `tests/place-definitions.test.js`, `tests/place-worlds.test.js` and `tests/districts.test.js` pass with the retired id rejected by save normalization.
- [x] 2.5 Delete the shared domain modules `shared/gardenModel.js`, `shared/crops.js`, `shared/materials.js` and `shared/economy.js`, and remove all remaining imports, material-node construction and node-state handling. Verify `rg` finds no importer and `npm run build` succeeds (final build gate in 6.2).

## 3. Node Sidecar

- [x] 3.1 Delete `server/gardens.js`, `server/economy.js`, `server/orderbook.js`, `server/nodes.js` and `server/machines.js`; remove their construction, imports, HELLO/WELCOME economy fields, message handlers, action results and 1 Hz garden tick from `server/index.js`, keeping the weather rotation and torrent housekeeping. Verify the server boots and `npm test` shows no missing-module failures.
- [x] 3.2 Trim `server/storage.js`: stop defaulting/normalizing gardens, orders, trades, multipliers, nodes, machines and player economy fields, and never rewrite the protected snapshot beyond what retained state requires. Verify `npm run p11:snapshot-hashes` still passes and `tests/multiplayer.test.js` passes without economy defaults.
- [x] 3.3 Update scripts and load tooling: remove the garden/market/contracts parity modules and fixtures (`scripts/parity/garden.mjs`, `market.mjs`, `contracts.mjs`, `tests/fixtures/parity/{garden-crops,market,contracts}.json`), regenerate the misc/identity fixture, and update `verify-world-runtime.mjs`, `verify-social-chat.mjs`, `measure-node-baseline.mjs` and `tools/load_client/*` to drop `market_buy`/`garden_state`/garden-room assumptions. Delete `scripts/bench-market-contention.mjs` with its mix task. Verify the affected scripts run and parity exports are deterministic.
- [x] 3.4 Delete domain tests (`tests/crops.test.js`, `tests/economy.test.js`, `tests/orderbook.test.js`, `tests/gardens_persistence.test.js`, `tests/crafting_gathering.test.js`, `tests/contracts.test.js`) and update `tests/multiplayer.test.js`, `tests/presence-race.test.js`, `tests/transport-adapter.test.js`, `tests/seating.test.js` and other garden-room fixtures to retained public rooms. Verify `npm test` is green.

## 4. Elixir / Phoenix

- [x] 4.1 Delete the `Afterlight.Gardens*`, `Afterlight.Economy*`, `Afterlight.EconomyGroup*`, `Afterlight.Restoration*`, `Afterlight.Import.EconomyGroup` and economy-only `Afterlight.Parity.*` modules, the economy ash domains in `config/config.exs`, the `EconomyGroup.Supervisor` child, the `AFTERLIGHT_ECONOMY_OWNER` routing branch and the economy mix tasks. Verify `mix compile --warnings-as-errors` succeeds.
- [x] 4.2 Move `Afterlight.World.Weather` under `World.Supervisor` and update `Gateway.Welcome`, `Protocol.Payloads`, `AfterlightWeb.GameChannel` and the durable/suppression lists to drop economy snapshots while keeping theater/IPTV/weather. Verify `mix test test/afterlight/gateway/welcome_test.exs test/afterlight_web/game_channel*`.
- [x] 4.3 Trim accounts normalization/import/reducer of garden/economy fields and update `Accounts` player plumbing to the retained profile shape; update the reducer-support nickname banks in lockstep with task 1.2. Verify `mix test test/afterlight/accounts`.
- [x] 4.4 Add the Ecto migration that drops the economy-group tables and the player economy columns, with a dated archive written before it runs; keep all prior migrations untouched. Verify `mix ecto.migrate` and rollback instructions against the test database.
- [x] 4.5 Update Elixir test support, parity reference modules and fixtures (`reference/garden.ex`, `reference/market.ex`, `reference/contracts.ex`, the economy portions of `reference/misc.ex`, `parity.ex` maps) and the game-channel/gateway/world tests that reference gardens. Verify `mix test` is green with database-tagged tests excluded.

## 5. Data Disposition and Documentation

- [x] 5.1 Implement the dated economy export to `.retired/economy-<UTC>.json` (gitignored) as a documented pre-drop step; verify the archive file exists and the three protected `data/*.json` files are untouched (`npm run p11:snapshot-hashes`).
- [x] 5.2 Regenerate the committed place projection with `node scripts/export-place-definitions.mjs` and confirm `node scripts/export-place-definitions.mjs --check` passes; verify personal-garden rejection and retired-id removal in `tests/place-projection.test.js`.
- [x] 5.3 Update `README.md`, `AGENTS.md`, `docs/places.md`, `docs/architecture/elixir/*` (ownership, cutover, protocol catalog) and any other docs that describe gardening, the market economy, P6 or the legacy gardener HUD; state the supersession of `add-ash-gardens-economy-restoration` and `deemphasize-legacy-farming`. Verify a repo-wide `rg -i "gardener|market garden|garden bed|produce|crop|sprinkler"` returns only historical/archived references and unrelated terms (`Rockgarden` course, `Mill Culvert` buoy, generic "gather").

## 6. End-to-End Verification

- [x] 6.1 `npm test` is green. (1362/1362 passing.)
- [x] 6.2 `npm run build` succeeds with only the known chunk-size warning. (Verified via `npx vite build`: the JS/CSS bundle builds with only the known chunk-size warning. The `build:wasm` pre-step could not run on this machine — no Rust/cargo toolchain — but the committed `public/wasm/afterlight_realtime.wasm` is unchanged and present; re-run `npm run build` on a machine with cargo before deploy.)
- [x] 6.3 `cd server_elixir && mix test` is green (database-tagged tests excluded as configured). (Verified: `mix test` → **880 tests, 0 failures, 19 excluded** on OTP 27 / Elixir 1.18.5 with a local Postgres on :5433. Also verified `mix ecto.create`/`mix ecto.migrate` for dev+test (the drop migration applies cleanly) and the pre-drop flow `mix ecto.migrate --to 20260909020000` → `mix afterlight.archive_economy` (16 tables archived to `.retired/`, gitignored) → drop migration. `mix compile --warnings-as-errors` still reports 27 pre-existing warnings in unrelated activity/theater/media modules (none in this change's modules); plain `mix compile` succeeds. Verification also surfaced four pre-existing/test-only issues, fixed here: catalog/theater import structs still set the dropped `coins_sum`/`xp_sum` fields; `payload_encoder_test` referenced the old fixture name; `welcome_test` double-started Weather now that World.Supervisor owns it; and an `entity_id_test` expectation assumed unsorted ids.)
- [x] 6.4 `npm run p11:snapshot-hashes` passes, proving the protected snapshots and their hashes are unchanged.
- [x] 6.5 Browser smoke on the dev server: spawn in The Orpheum, travel via T to The Rain Court and the Market Court, walk the canal↔station gate route, confirm no tool belt/economy pills/satchel/market/mill surfaces, the profile is nickname-only, an industrial nickname renders, and the console shows no runtime errors.
- [x] 6.6 Mixed-version tolerance: send a retired command (e.g. `garden_action`) and feed a retired snapshot to a live session; confirm a bounded error/no-op and no client error toast.
- [x] 6.7 Confirm the supersession notes: no edits to `openspec/changes/add-ash-gardens-economy-restoration/**` or `openspec/changes/deemphasize-legacy-farming/**`, and the proposal/design record their cancellation.

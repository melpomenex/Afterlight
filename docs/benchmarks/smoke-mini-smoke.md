# Benchmark — smoke-mini (placeholder)

**Status:** not yet run — gateway was unavailable during assembly  
**Recorded:** 2026-09-07 (structure only)

This report slot exists for the smoke-mini scenario. Re-run after starting the stack:

```sh
npm run dev:stack
npm run load:smoke
```

Expected captures at 2 sessions: join p50/p95, frame interval ~100 ms, tick consume lag, JSON payload byte p50/p95/p99.

See [runbook](./runbook.md) for full parameters.
